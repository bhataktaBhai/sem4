\let\emph\textsl
\input{~/IISc/sem4/preamble}
\input{~/IISc/sem4/preamble/flowchart}
\usepackage{forest}
\newcommand\size[1]{\# #1}
\newcommand\comp[1]{\overline{#1}}
\DeclareMathOperator{\lengths}{lengths}
\newcommand\follows\xrightarrow
% \usepackage[bitstream-charter]{mathdesign}
% \renewcommand*{\rmdefault}{cmr}

\usepackage{tikz}
\usetikzlibrary{automata,positioning,arrows,calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\tikzset{
    >=stealth', % makes the arrow heads bold
    node distance=1cm and 1.155cm, % specifies the minimum distance between two nodes. Change if necessary.
    every state/.style={thick, fill=gray!10}, % sets the properties for each 'state' node
    initial text=$ $, % sets the text that appears on the start arrow
    auto,
    shorten >=1pt,
}
% \makeatletter
% \renewcommand\xRightarrow[2][]{%
%     \ext@arrow 0055{\Rightarrowfill@}{#1\ }{#2\ }}
% \makeatother
\usepackage{pifont}% http://ctan.org/pkg/pifont
\newcommand{\cmark}{\ding{51}}%
\newcommand{\xmark}{\ding{55}}%
